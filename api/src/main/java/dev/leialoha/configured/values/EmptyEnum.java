package dev.leialoha.configured.values;

public enum EmptyEnum {
    // This enum is intentionally empty and serves as a placeholder for cases where an enum is required.
    // It can be used in annotations or configurations where an enum type is expected, but no specific values are needed.
}
